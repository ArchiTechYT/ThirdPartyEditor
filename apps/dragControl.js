import * as THREE from "three";
import { OrbitControls } from "three/addons/controls/OrbitControls.js";

// to have a smoother UX while dragging elements
// [1] to make the dragging & dropping snap to base grid, there needs to be an min snapping element to form the grid

//
